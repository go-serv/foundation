syntax = "proto3";

import "google/protobuf/descriptor.proto";

package net.cerberus;

extend google.protobuf.ServiceOptions {
  // Close current session after the given call.
  repeated string requires_role = 60200;
}
