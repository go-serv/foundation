version: "3.9"

services:
  goserv-tests-server:
    build:
      context: .
    env_file:
      - .env
    volumes:
      - type: bind
        source: ./src/server
        target: /app/target
      - type: bind
        source: ./assets/certs
        target: /share/certs
